import React from 'react'
import ReactDOM from 'react-dom/client'
import DocumentAuthenticator from './App.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <DocumentAuthenticator />
  </React.StrictMode>,
)
